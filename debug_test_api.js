const fetch = require('node-fetch');

async function run() {
  try {
    const payload = {
      mhsId: '66a1e38c9ad398fdd6f481c1', // Provide a valid ID from the DB
      tipe_laporan: 'individu', // or pokja
      status: 'submitted'
    };
    
    // We don't have the dev server URL. 
  } catch(e) {}
}
