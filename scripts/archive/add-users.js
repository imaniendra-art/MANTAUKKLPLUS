import mongoose from 'mongoose';
import bcrypt from 'bcryptjs';
import User from './models/User.js'; // Might need to adjust path or use dynamic import if module issues
// Since it's a script we might need to connect manually and use require or adjust type
